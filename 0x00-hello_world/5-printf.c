#include <stdio.h>
/**
 * main -Prints a line of code with the printf function
 *
 * Return: 0(Successful)
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
