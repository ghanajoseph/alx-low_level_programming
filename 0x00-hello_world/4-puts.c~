#include <stdio.h>

/**
 * main - A C program that prints using the puts function
 *
 * return: returns 0 (that is successful)
 */

int main(void)
{
  puts("\"Programming is like building a multilingual puzzle");
  return (0);
}
