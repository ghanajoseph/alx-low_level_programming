#include <stdio.h>
/**
 * main -Prints integers in base
 *
 * Return: 0(Successful)
*/

int main(void)
{
	int a;
	/* This is a loop */
	for (a = 0; a <= 10; a++)
	{
		printf("%d", a);
	}
	putchar('\n');
	return (0);
}
