#include <stdio.h>
/**
 * main -Prints a line of code
 *
 * Return: 0(Successful)
*/

int main(void)
{
	char c;

	c = 'z';
	/* This is the loop */
	while
		(c >= 'a')
		{
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
